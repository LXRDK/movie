import React from "react";
import { Text, View } from "react-native";

const MovieCard = () => {
  return (
    <View>
      <Text className="text-white">MovieCard</Text>
    </View>
  );
};

export default MovieCard;
